---
- hosts: all
  become: true
  tasks:
  
  - name: update repo index
    apt:
      update_cache: yes

  - name: install Apache
    apt:
      name: apache2
      state: present

  - name: run Apache
    command: apache2ctl start

